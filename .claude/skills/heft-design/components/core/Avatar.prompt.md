Circular user avatar; shows the image or falls back to initials.

```jsx
<Avatar name="Liana Tudakova" size="md" ring />
```

Sizes `xs`/`sm`/`md`/`lg`. Pass `src` for a photo, otherwise `name` renders initials on a forest-green chip. `ring` adds an emerald focus ring.
